import XCTest
@testable import PlanningProvider

final class PlanningProviderTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PlanningProvider().text, "Hello, World!")
    }
}
